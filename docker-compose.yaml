version: '3.8'

services:
  websocket-api:
    build: .
    expose:
      - 8001
    networks:
      - projects
    command: gunicorn
    container_name: websocket-api
    restart: unless-stopped

networks:
  projects:
    external: true